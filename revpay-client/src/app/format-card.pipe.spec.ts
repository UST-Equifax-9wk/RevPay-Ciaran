import { FormatCardPipe } from './format-card.pipe';

describe('FormatCardPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCardPipe();
    expect(pipe).toBeTruthy();
  });
});
